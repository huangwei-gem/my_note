# GitHub一键提交脚本使用说明

## 功能说明

这个脚本可以一键将你的笔记提交到GitHub仓库的`outside`分支，具有以下特性：

### 主要功能
1. **自动分支管理**: 自动创建并切换到`outside`分支
2. **SSH优先连接**: 优先使用SSH方式连接GitHub，如果失败则自动切换到HTTPS
3. **大文件过滤**: 自动扫描并忽略大于50MB的文件，避免提交失败
4. **智能提交**: 只在有文件变更时才进行提交
5. **用户信息配置**: 自动配置Git用户信息

### 脚本文件

1. **git-push-outside.cmd** - 完整版脚本
   - 功能最全，有详细的错误处理和状态显示
   - 适合日常使用，可以看到详细的执行过程

2. **git-push-outside-simple.cmd** - 简化版脚本
   - 代码更简洁，执行速度更快
   - 适合熟悉操作后使用

## 使用方法

### 首次使用
1. 双击运行 `git-push-outside.cmd`
2. 脚本会自动：
   - 检查Git环境
   - 配置用户信息
   - 初始化仓库（如果需要）
   - 创建`outside`分支
   - 扫描并忽略大文件
   - 提交到GitHub

### 日常使用
- 直接双击运行脚本即可
- 脚本会自动处理所有变更

## 注意事项

### SSH连接配置
确保你的SSH key已经配置好：
```bash
# 检查SSH key
ls ~/.ssh/id_rsa.pub

# 如果没有，生成新的SSH key
ssh-keygen -t rsa -b 4096 -C "3579628804@qq.com"

# 添加SSH key到GitHub
# 复制公钥内容
cat ~/.ssh/id_rsa.pub
# 然后添加到GitHub账户的SSH keys设置中
```

### 大文件处理
- 脚本会自动扫描大于50MB的文件
- 这些文件会被自动添加到`.gitignore`文件中
- 如果需要提交大文件，考虑使用Git LFS

### 分支说明
- 默认提交到`outside`分支
- 如果分支不存在会自动创建
- 不会影响到其他分支

### 错误处理
常见错误及解决方案：

1. **Git未安装**: 安装Git并添加到系统PATH
2. **SSH连接失败**: 检查SSH key配置或网络连接
3. **推送失败**: 检查网络连接和仓库权限
4. **冲突问题**: 手动解决冲突后重新运行

## 仓库信息
- **仓库地址**: https://github.com/huangwei-gem/my_note
- **目标分支**: outside
- **用户名**: huangwei-gem
- **邮箱**: 3579628804@qq.com

## 更新日志
- 2025-12-09: 创建脚本，支持SSH/HTTPS自动切换，大文件过滤